# AUR packages. Installed by `bootstrap-cachyos` via:
#   shelly aur install <pkgs>
#
# To check this list against installed packages, run `./sync-packages`.
# It reports drift and only ever appends, so the comments and sections below survive.
#
# NOTE: AUR packages are community-maintained. Read the PKGBUILD before
# installing — `shelly aur search-pkgbuild <pkg>` prints it — especially for
# anything that pulls a binary from a third-party URL.

claude-desktop         # repackages Anthropic's official `.deb` verbatim
zen-browser-bin        # Web browser. See: https://zen-browser.app/
proton-pass-bin        # repackages Proton's official .deb
sddm-silent-theme      # Login manager theme. See: https://github.com/uiriansan/SilentSDDM
bolt-launcher          # Third-party RuneScape launcher
openssl-1.1            # Requirement of Bolt
protonmail-bridge-bin  # Required for Proton Mail open in Thunderbird mail client
